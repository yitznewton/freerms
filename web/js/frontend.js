$(function (){
  //var metaRefresh = $('meta
  
  if (navigator.appName == 'Microsoft Internet Explorer' && ) {
    // hack to get IE to pass Referer URL
    link.click();
  }
  else {
    window.location = link.href;
  }
});

